$fa = 1;
$fs = 0.4;

// LED strip length (incl. margins)
strip_l = 265;
// LED strip width (incl. margins)
strip_w = 11;

// Number of arms
arms = 8;
// Arm height
arm_h = 5;
// Outer arm radius
arm_r = round(strip_l*2/PI);

// Connector margin
connector_margin = 0.05;

// Outer hub radius
hub_r = 30;
// Hub width (excl. center hole)
hub_w = 20;
// Hub height
hub_h = 15;

// Height of the rail, in addition to the arm
rail_h = 8;
// Width of the rail
rail_w = 2;
